# Rust CLI contract migration 2026-04-24

This note records the JSON contract policy for migrating from the JavaScript `tradingview-mcp` CLI to this Rust-native `tv` CLI.

## Decision

The Rust CLI keeps its structured envelope:

```json
{
  "success": true,
  "command": "quote",
  "data": {}
}
```

Error output uses the matching structured error envelope:

```json
{
  "success": false,
  "command": "quote",
  "error": {
    "kind": "connection",
    "message": "CDP connection failed",
    "details": null
  }
}
```

This is intentionally different from the JavaScript CLI, which generally returned command payload fields at the top level:

```json
{
  "success": true,
  "symbol": "NASDAQ:AAPL"
}
```

## Rationale

The Rust envelope is easier to process consistently across commands. It gives downstream callers stable places for command name, success state, command payload, error kind, error message, and optional details.

The compatibility cost is real: adapters that read old top-level command fields must read `data` instead. This is an accepted breaking wire-format change.

## Information compatibility rule

The wire shape may change, but migrated commands must preserve practical information compatibility.

If the old JavaScript CLI exposed useful information for a command, the Rust implementation of that command must make the same information available unless a durable project decision explicitly accepts the loss. New fields are allowed. Field names may change when the replacement is documented, but silently dropping useful information is not allowed.

When an old field name is important to downstream workflows, prefer one of these outcomes:

- keep the old field name inside `data`
- add a clearly documented replacement field inside `data`
- record why the information is unavailable and get an explicit decision before merging

## Downstream adapter migration checklist

- Continue reading top-level `success`.
- Read successful command payloads from top-level `data`.
- Read failure text from `error.message`.
- Use `error.kind` for structured handling where useful.
- Use top-level `command` for logging, diagnostics, and command correlation.
- Do not assume the Rust CLI is a drop-in JSON wire-format clone of the JavaScript CLI.
- When multiple TradingView chart targets are open, use `tv tab list` to read target ids and set `TV_CDP_TARGET_ID` for chart-specific commands. Without that override, ambiguous chart targets still produce `target_ambiguous`.

## Current known differences

- `status` payload is under `data` in Rust and now includes CDP target fields plus chart API availability and current chart fields such as `chart_symbol`, `chart_resolution`, `chart_type`, and `api_available`.
- `launch` payload is under `data` in Rust and includes `launched`, `used_existing`, `platform`, `binary`, `pid`, `cdp_port`, `cdp_url`, `cdp_ready`, `browser`, `user_agent`, `kill_existing`, `launch_method`, `resolved_by`, `fallback_used`, and optional `warning`. Unlike the old JavaScript CLI, Rust defaults to no-kill behavior and only attempts to terminate existing TradingView processes when `--kill-existing` is explicit. Windows launch resolution includes a bounded PowerShell check for running-process and AppX/MSIX install paths. macOS launch first tries direct spawn and then falls back to `open -a TradingView --args ...` if CDP does not become ready.
- `state` payload is under `data` in Rust and includes both old and new naming where useful, including `resolution`, `timeframe`, `chartType`, `chart_type`, `studies`, and `visible_range`.
- `quote [SYMBOL]` payload is under `data` in Rust and includes the practical quote fields from the old CLI shape, including `symbol`, `time`, `last`, `close`, `open`, `high`, `low`, `volume`, and best-effort symbol metadata. Without `SYMBOL`, Rust reads the current chart as before. With `SYMBOL`, Rust may temporarily switch the active chart, read the requested symbol, and restore the original chart symbol before reporting success. Symbol-targeted quote commands use a short process lock to avoid concurrent switch/restore races against the same local TradingView session. Payloads include `requested_symbol`, `original_symbol`, `observed_symbol`, `switch_performed`, and `restored` so callers can distinguish current-chart reads from temporary mutation reads.
- `ohlcv --summary` payload is under `data` in Rust and includes summary fields such as `symbol`, `timeframe`, `bar_count`, `period`, `range`, `change`, `change_pct`, `avg_volume`, `volume`, and `last_5_bars`.
- Raw `ohlcv --count`, `range`, and `scroll` are implemented in Rust. Their payloads still live under `data`.
- `info` payload is under `data` in Rust and includes symbol metadata such as `symbol`, `full_name`, `exchange`, `description`, `type`, `pro_name`, `typespecs`, `resolution`, and `chart_type`.
- `search` payload is under `data` in Rust and includes `query`, `source`, `count`, and normalized `results` rows with `symbol`, `description`, `exchange`, `type`, and `full_name`.
- `scanner hotlist <SLUG> [--limit <N>]` is implemented in Rust as a read-only upstream-follow-up command, not as old JavaScript CLI migration parity. Its payload lives under `data` and includes `source: "scanner_preset_rest"`, `region`, `slug`, `limit`, `count`, `total_count`, `fields`, and normalized `symbols`. Each symbol row preserves the compact scanner values under `values` and maps known field names to values under `field_values`. The command validates a fixed Hotlist slug whitelist before network access, rejects `--limit 0`, and caps larger limits at 20.
- `scanner scan [--market america] [--exchange <EXCHANGE>...] [--sector <TEXT>...] [--industry <TEXT>...] [--type <TEXT>...] [--subtype <TEXT>...] [--columns <CSV>] [--sort <FIELD>] [--asc|--desc] [--limit <N>]` is implemented in Rust as a read-only generic TradingView scanner REST command. Its payload lives under `data` and includes `source: "scanner_scan_rest"`, `market`, `limit`, `count`, `total_count`, `columns`, `sort`, `filters`, and normalized `symbols`. It supports numeric filters such as `--min-price`, `--max-price`, `--min-volume`, `--min-market-cap`, `--min-change`, `--max-change`, `--min-relative-volume`, `--max-pe`, `--min-average-volume`, `--min-performance-week`, `--max-performance-week`, `--min-performance-month`, `--max-performance-month`, `--min-performance-quarter`, `--max-performance-quarter`, `--min-rsi`, `--max-rsi`, `--min-recommendation`, and `--max-recommendation`. Signed daily-change, performance, and recommendation values are accepted where their scanner fields allow negative values. It also supports `type`, `subtype`, `average_volume_10d_calc`, `Perf.W`, `Perf.1M`, `Perf.3M`, `RSI`, and `Recommend.All` as scanner columns. The command validates market, fields, sort, order flags, blank string filters, finite numeric inputs, field-specific ranges, and `--limit 0` before network access. It currently supports only the `america` scanner market and caps larger limits at 100.
- `screener status`, `screener open`, `screener get [--limit <N>]`, `screener screens active/actions/list/switch/save/create/rename/save-as/delete`, `screener filters list/actions/add/modify/remove/clear`, `screener columns list/actions/remove`, and `screener close` are implemented in Rust as UI Screener dialog commands. Their payloads live under `data` and use `source: "ui_screener_dialog"`. `status` reports whether a visible Screener panel root appears open, whether the toolbar button was found, and visible counts such as columns, filters, and rows; toolbar buttons, unrelated right-panel content, and off-viewport tables do not count as an open Screener panel. `open` and `close` may change visible UI state but do not save screens, remove filters, or change columns. `get` reads localized display text from the currently visible Screener table and returns `dialog_title`, `screen_title`, `columns`, `filters`, `rows`, `row_count`, `visible_row_count`, `limit`, `opened_for_read`, and `restored_open_state`. `screens active` returns the current dialog and screen titles. `screens actions` reads active screen title menu actions and returns `scope: "screen_title_menu"`, `actions`, `action_count`, `save_available`, `save_enabled`, `opened_for_read`, and `restored_open_state`. `screens list` reads screen entries visible in the active screen title menu and returns `scope: "screen_title_menu"`, `screen_title`, `screens`, `screen_count`, `catalog_entry_found`, `opened_for_read`, and `restored_open_state`; `screens list --catalog` opens the saved-screen catalog and returns `scope: "screen_catalog"`, `screen_title`, `screens`, `screen_count`, `opened_for_read`, and `restored_open_state`. `screens switch --name <NAME> [--dry-run] [--catalog]` is an explicit Screener UI mutation that targets an exact visible screen name from either the title menu or saved-screen catalog, returns `target_screen`, before/after screen titles, `scope`, `switched`, `already_active`, `dry_run`, `opened_for_mutation`, and `restored_open_state`, and rejects blank or non-visible names before clicking a screen. `screens save [--dry-run]` is an explicit Screener UI mutation that targets only the exact visible `Save screen` / `スクリーンを保存` action; dry-run returns `target_action` without clicking, while normal mode clicks the action and returns `clicked`, `save_requested`, `confirmation`, `after_screen_title`, and blocking-dialog post-check fields without claiming more than the observable UI result. `screens create --name <NAME> [--dry-run]`, `screens rename --name <CURRENT> --to <NEW> [--dry-run]`, and `screens save-as --name <NAME> [--dry-run]` are guarded Screener lifecycle mutations. Normal mode is limited to test/disposable screen names containing `CLI-Test` or `テスト`, drives the visible screen-title menu name dialog, and reports success only after the active screen title matches the requested result. `screens delete --name <NAME> [--dry-run] --confirm-delete` currently supports exact catalog target resolution in dry-run mode; normal deletion fails with `internal_api_unavailable` until a safe exact-screen delete action and confirmation path are verified. `filters list` returns visible filter metadata with `index`, `text`, `data_name`, and `visible`. `filters actions` opens the visible filter edit UI and reports add button presence, numeric preset-edit availability, candidate filter text, target-popover-scoped range options, and unavailable reasons without saving screen state. `filters add --name <TEXT> --min <N>|--max <N> [--dry-run]` searches the visible add-filter catalog, selects a visible numeric range preset, returns `target_filter` and `requested_range`, validates finite input before CDP connection, and verifies that a new visible filter pill appears before reporting success. `filters modify --index <N>|--text <TEXT> --min <N>|--max <N> [--dry-run]` targets one visible numeric range filter, maps finite numeric input to currently supported visible presets such as `0% 〜 10%` or `10%以上`, returns `target_filter` and `requested_range`, treats already-matching presets as a no-op, and verifies visible filter text after normal mutation before reporting success. `filters remove --index <N>|--text <TEXT> [--dry-run]` is an explicit Screener mutation that targets one visible filter, returns `target_filter`, before/after counts, `removed`, `dry_run`, `opened_for_mutation`, and `restored_open_state`, and rejects missing, conflicting, or ambiguous selectors before mutation. `filters clear [--dry-run] --confirm-clear` removes all visible filters only when confirmed; dry-run returns `target_filters` without mutation. `columns list` returns visible column metadata with `index` and `name`. `columns actions` opens the visible column settings surface and returns detected categories plus `remove_supported`, `reset_supported`, and an `unavailable_reason` when no safe remove/reset action is visible. `columns remove --index <N>|--name <TEXT> --dry-run` resolves exactly one visible column and returns the target without mutation; normal column removal currently fails with `internal_api_unavailable` when the visible UI exposes no safe per-column remove action. Read commands that need the dialog may open it temporarily and then restore the original open or closed state. `get` rejects `--limit 0` before CDP connection, defaults to 20 rows, and caps larger limits at 100.
- `values` payload is under `data` in Rust and includes `study_count` plus `studies` rows with `name` and `values`.
- `discover` payload is under `data` in Rust and includes `apis_available`, `apis_total`, and `apis`.
- `ui-state` payload is under `data` in Rust and includes panel state, button groups, key buttons, chart summary, and replay state.
- `watchlist get`, `watchlist add`, `watchlist add-bulk`, `watchlist remove`, `pane list`, `pane layout`, `pane focus`, and `pane symbol` are implemented in Rust. Their payloads still live under `data`; `watchlist get` may return `source: "panel_closed"` with `count: 0` when the watchlist panel is closed.
- `watchlist add` is an explicit operator mutation. It preserves the practical old CLI fields `symbol` and `action` under `data`, with Rust-specific context such as `requested_symbol`, `source`, `opened_panel`, `add_button`, `before_count`, `after_count`, `matched_before`, `matched_after`, and `click_method` also available. Rust treats an already-present exact symbol as `action: "already_present"` and verifies a newly added symbol is visible in the watchlist after input before returning success.
- `watchlist add-bulk <SYMBOL>... [--delay-ms <MS>] [--allow-partial]` is an explicit operator mutation implemented as a bounded sequential wrapper around verified `watchlist add`. Its payload lives under `data` and includes `action: "bulk_add"`, requested and processed counts, added/already-present/failed/duplicate counts, `delay_ms`, `allow_partial`, and per-input `results`. Without `--allow-partial`, the command attempts every unique symbol and returns an error with the same aggregate payload in `error.details` if any symbol fails. With `--allow-partial`, failures are reported inside `data.results` and the command exits successfully.
- `watchlist remove` is a Rust-specific cleanup command rather than old CLI parity. Its payload lives under `data` and includes `symbol`, `requested_symbol`, `action`, `removed`, `source`, `before_count`, `after_count`, `matched_before`, `matched_after`, `remove_method`, and `confirmation_clicked`. It verifies the symbol exists before deletion and verifies the symbol is gone after deletion.
- `pane layout`, `pane focus`, and `pane symbol` are explicit chart mutations. They preserve the practical old CLI fields such as `layout`, `layout_name`, `chart_count`, `panes`, `focused_index`, `total_panes`, `index`, and `symbol` under `data`. `pane layout` validates supported old CLI layout codes and aliases before connecting to CDP.
- `pane layout`, `pane focus`, and `pane symbol` depend on TradingView's current internal chart widget collection. If the current page does not expose that collection or the requested pane index is unavailable, Rust reports `internal_api_unavailable`.
- `layout list` is implemented in Rust as a read-only saved chart layout command. Its payload lives under `data` and includes `layout_count`, `source`, `layouts`, and optional `error`; layout rows preserve the old practical fields `id`, `name`, `symbol`, `resolution`, and `modified`.
- `layout switch <TARGET> [--dry-run]` is implemented in Rust as an explicit saved chart layout mutation. Its payload lives under `data` and includes `action`, `dry_run`, `target`, `layout`, `layout_id`, optional `layout_url`, `source`, `method`, `navigation_expected`, `layout_count`, `unsaved_dialog_observed`, and `unsaved_dialog_dismissed`. Rust resolves by layout id or exact case-insensitive name, not by old CLI partial matching, and it does not automatically dismiss unsaved-change dialogs. When TradingView exposes a saved layout URL, Rust navigates to that chart URL because current TradingView Desktop builds can report success from the old `loadChartFromServer(id)` path without visibly loading the layout.
- `alert list` is implemented in Rust. Its payload lives under `data` and includes `alert_count`, `source`, `alerts`, and optional `error`; alert rows preserve practical old CLI fields such as `alert_id`, `symbol`, `type`, `message`, `active`, `condition`, `resolution`, `created`, `last_fired`, and `expiration`.
- `alert list` depends on the current TradingView page session and the internal `pricealerts.tradingview.com/list_alerts` endpoint. When the page fetch fails, Rust preserves the failure as `data.error` with an empty alert list rather than treating the read command itself as a CDP failure.
- `alert create` is implemented in Rust as an explicit account mutation. Its payload lives under `data` and preserves the practical old CLI fields `price`, `condition`, `message`, `price_set`, and `source`; Rust also reports `created`, `opened`, `open_selector`, and `message_set` when available. The command validates finite price values and the supported conditions `crossing`, `greater_than`, and `less_than` before connecting to CDP.
- `alert create` keeps downstream workflow policy out of the core CLI. Duplicate detection, chart symbol setup, chart symbol restoration, and dry-run or apply decisions remain downstream responsibilities.
- `alert delete --id` is implemented in Rust as an explicit account mutation. Its payload lives under `data` and includes `alert_id`, `deleted`, `source`, `before_count`, `after_count`, `matched_before`, `matched_after`, and `matched_alert`. It verifies the alert exists before deletion and verifies the ID is gone after deletion.
- `alert delete --all [--dry-run]` is implemented in Rust as an explicit bulk account mutation. Its payload lives under `data` and includes `action`, `dry_run`, `deleted`, `source`, `before_count`, `after_count`, `target_alert_ids`, `target_alerts`, and `remaining_target_alert_ids`. `--dry-run` reports targets without deleting. Normal mode posts all listed ids to the internal delete endpoint and requires those target ids to be absent afterward.
- `draw shape`, `draw position`, `draw list`, `draw get`, `draw remove`, and `draw clear` are implemented in Rust. Their payloads live under `data`; `draw shape` exposes `action`, `shape`, `entity_id`, `new_shape_count`, `before_count`, `after_count`, `point`, `point2`, `text`, and `override_count`; `draw position` exposes `action`, `direction`, `shape`, `entity_id`, `new_shape_count`, `before_count`, `after_count`, `entry_price`, `stop_loss`, `take_profit`, `entry_time`, `stop_level`, `profit_level`, `risk_reward_ratio`, optional `account_size`, `risk`, `lot_size`, and `source`; `draw list` exposes `count` and `shapes`; `draw get` exposes drawing properties when available; `draw remove` exposes `action`, `entity_id`, `removed`, `before_count`, and `remaining_shapes`; `draw clear --dry-run` exposes `action: "dry_run"`, `dry_run`, `before_count`, `would_clear_count`, `cleared_entities`, and `source`; normal `draw clear` exposes `action`, `cleared`, `before_count`, `after_count`, `cleared_entities`, and `source`.
- `draw clear` preserves the old JavaScript CLI's all-shapes removal capability, but adds a read-only dry-run and post-delete verification. If `removeAllShapes()` leaves any drawings behind, Rust reports `internal_api_unavailable` instead of a successful clear.
- `data indicator`, `data strategy`, `data trades`, `data equity`, `data lines`, `data labels`, `data tables`, `data boxes`, `data shapes`, and `data depth` are implemented in Rust. Their payloads live under `data`; the practical old CLI fields such as `metric_count`, `trade_count`, `data_points`, `study_count`, `studies`, `inputs`, `metrics`, `trades`, `equity_summary`, `bid_levels`, `ask_levels`, `spread`, `bids`, and `asks` remain available. `data labels` defaults to 500 returned labels when `--max` is omitted and reports per-study `available_labels`, `limit`, and `truncated` so callers can detect omitted older labels. `data shapes` is a Rust follow-up to upstream PR #35 and reads visible Pine `plotshape()` / `plotchar()` signal metadata such as shape plots, signal count, scanned bars, bar index, value, timestamp, and OHLC when TradingView exposes them. Strategy data reads prefer the TradingView `StrategyScript` marker and `_reportData` paths when available, while preserving legacy `reportData`, `ordersData`, `equityData`, and visible Strategy Tester DOM fallbacks.
- `data depth` remains DOM-panel dependent. If TradingView's DOM or Depth of Market panel is closed or does not expose readable rows, the command may fail with `internal_api_unavailable` rather than returning an empty success payload.
- `type [CHART_TYPE]` is implemented in Rust. Its payload lives under `data`; the practical old CLI fields `chart_type` and `type_num` remain available. Set-mode payloads also include requested, previous, and observed chart type fields so callers can restore the original chart type after smoke or screenshot workflows.
- `replay start`, `replay step`, `replay stop`, `replay status`, `replay autoplay`, and `replay trade` are implemented in Rust. Their payloads live under `data`; `status` preserves the practical old CLI fields `is_replay_available`, `is_replay_started`, `is_autoplay_started`, `replay_mode`, `current_date`, `autoplay_delay`, `position`, and `realized_pnl`. Basic control payloads include practical fields such as `action`, `replay_started`, `date`, `previous_date`, and `current_date`. `replay autoplay` preserves the practical old CLI fields `autoplay_active` and `delay_ms`, adds `requested_delay_ms`, and validates optional speed before connecting. `replay trade` preserves the practical old CLI fields `action`, `position`, and `realized_pnl`.
- `stream quote`, `stream bars`, `stream values`, `stream lines`, `stream labels`, `stream tables`, and `stream all` are implemented in Rust. Unlike ordinary commands, stream commands print newline-delimited JSON envelopes, one line per changed sample. Each line still uses the Rust envelope with `success`, `command: "stream"`, and `data`. Stream payloads include `_stream` and `_ts` plus practical old CLI fields such as `symbol`, `time`, `open`, `high`, `low`, `close`, `volume`, `resolution`, `bar_time`, `bar_index`, `study_count`, `studies`, `layout`, `pane_count`, and `panes`.
- `tab list`, `tab switch`, `tab new`, and `tab close` are implemented in Rust. Their payloads live under `data`; `tab list` exposes old-compatible chart-target fields `tab_count` and `tabs`, plus Rust-specific `app_tab_count` and `app_tabs` for the TradingView Desktop tab strip. It also exposes `screener_target_count` and `screener_targets` for full-page TradingView Screener targets, with each target carrying `target_env.TV_CDP_TARGET_ID` so operators can run `tv screener ...` against a Screener tab instead of a chart-side Screener panel. `tab switch` exposes `action`, `index`, `tab_id`, `target_id`, `target_env`, `next_command_hint`, `chart_id`, `title`, and `url`; `target_env.TV_CDP_TARGET_ID` is the explicit handoff value for the next chart-specific command when multiple chart targets are open. `tab new` exposes `action`, `source_index`, `source_tab`, `tabs_before`, `tabs_after`, `app_tabs_before`, `app_tabs_after`, `new_app_tabs`, `chart_tabs_before`, `chart_tabs_after`, and `new_tabs`. `tab close` exposes `action`, `closed_index`, `closed_tab`, `tabs_before`, `tabs_after`, `app_tabs_before`, `app_tabs_after`, `chart_tabs_before`, and `chart_tabs_after`. `tab close` is intentionally safer than the old CLI: it requires an explicit app-tab index and refuses to close the final app tab.
- `pine get`, `pine set`, `pine compile`, `pine raw-compile`, `pine save`, `pine analyze`, `pine check`, `pine new`, `pine open`, `pine errors`, `pine console`, and `pine list` are implemented in Rust. Their payloads live under `data`; `pine get` exposes `source`, `line_count`, `char_count`, `editor_open_before`, and `opened_editor`; `pine set` exposes `lines_set`, `char_count`, `input_source`, `editor_open_before`, and `opened_editor`; `pine compile` exposes `button_clicked`, `has_errors`, `error_count`, `errors`, `study_added`, `studies_before`, `studies_after`, `editor_open_before`, and `opened_editor`; `pine raw-compile` exposes `button_clicked`, `source`, `editor_open_before`, `opened_editor`, and `raw_compile`, and intentionally preserves the old broad button behavior that may click save-related Pine actions; `pine save` exposes `saved`, `action`, `name`, `dialog_handled`, `source`, `editor_open_before`, `opened_editor`, `dirty_before`, and `dirty_after`, and is limited to persistence for an already saved current script; `pine analyze` exposes `input_source`, `issue_count`, `diagnostics`, and optional `note`; `pine check` exposes `input_source`, `compiled`, `error_count`, `warning_count`, `errors`, `warnings`, `source`, and optional `note`; `pine new` exposes `type`, `action`, `template`, `lines_set`, `char_count`, `editor_open_before`, and `opened_editor`; `pine open` exposes `name`, `script_id`, `version`, `lines`, `source`, `opened`, `editor_open_before`, and `opened_editor`; `pine errors` exposes `has_errors`, `error_count`, `errors`, `editor_open_before`, and `opened_editor`; `pine console` exposes `entries`, `entry_count`, `editor_open_before`, and `opened_editor`; `pine list` exposes `scripts`, `count`, `source`, and optional `error`. Explicit named unsaved save remains deferred because current TradingView Desktop live smoke showed the naming dialog can be outside the CDP target; `pine save` must fail rather than keyboard-type into an unverified focus target when that dialog appears.
- `ui click`, `ui keyboard`, `ui hover`, `ui scroll`, `ui find`, `ui eval`, `ui type`, `ui panel`, `ui fullscreen`, and `ui mouse` are implemented in Rust as generic old CLI compatibility commands. Their payloads live under `data`, preserve the practical clicked/found/typed/panel/action fields, and can mutate the active TradingView UI session. `ui eval` is disabled by default because it runs arbitrary JavaScript in the authenticated TradingView page context; set `TV_ALLOW_UNSAFE_UI_EVAL=1` to enable that unsafe compatibility command explicitly. When enabled, `ui eval` adds `unsafe_eval_enabled: true` to its success payload.
- `screenshot --region full` and `screenshot --region chart` are implemented in Rust. Their payloads live under `data`; screenshot payloads include the old CLI practical fields `method`, `file_path`, `region`, and `size_bytes`, plus Rust's `output_path`. Chart-region screenshot payloads also include `clip` fields with `x`, `y`, `width`, `height`, and `scale`, and `capture_mode`.
- Chart-region screenshots prefer CDP `Page.captureScreenshot` with a `clip` parameter, matching the old JavaScript CLI's practical capture path. If clipped CDP capture fails, Rust falls back to a full-page CDP screenshot plus local PNG crop.
- A clipped CDP timeout was observed during development, but later checks showed both the old JavaScript CLI and the Rust implementation could capture the same chart through clipped CDP. Treat that timeout as an intermittent CDP/session-state observation, not as a confirmed TradingView or Rust library limitation.
- `screenshot --region chart` remains DOM-selector dependent. If TradingView changes the visible chart DOM or no chart element is available, the command may fail with `internal_api_unavailable`.
- The old JavaScript CLI included `success: true` inside `discover` and `ui-state` payloads. Rust does not duplicate that field inside `data`; success remains the top-level envelope field.

These differences are migration gaps, not proof that the information is out of scope.
