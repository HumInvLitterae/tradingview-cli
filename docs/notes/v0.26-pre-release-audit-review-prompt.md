# Focused re-review prompt: v0.26 pre-release audit corrections

Perform a read-only focused re-review of two documentation corrections from the
v0.26 pre-release completion and architecture audit. Do not edit files, stage
changes, commit, or push. The first review already approved the Gate 1 through
6 implementation and the conclusion that no dedicated pre-release refactor is
required; do not repeat the full architecture review unless these corrections
expose a new contradiction.

The review base remains commit `f23a0d5`. Production Rust code, Cargo manifests,
Cargo lockfile, and public contracts must remain unchanged.

Read these current sources first:

- `docs/plans/tradingview-cli-v0.26-pre-release-audit.md`
- `docs/v0.26-roadmap.md`
- `docs/v0.26-work-items.md`
- `docs/plans/README.md`
- `CONTINUITY.md`
- `CHANGELOG.md`

Check only these findings and their documentation consequences:

- all ten executable user-specific Codex validator paths in the three
  identified archived plans use the portable
  `${CODEX_HOME:-$HOME/.codex}/...` form;
- the command syntax remains usable with either explicit `CODEX_HOME` or the
  default `$HOME/.codex` location;
- the audit correctly states that `quote.rs` production code continues through
  line 481 and its test module begins at line 482; the line 295 `#[cfg(test)]`
  applies only to one helper;
- the audit Progress, Surprises, Outcomes, revision notes, and `CONTINUITY.md`
  consistently report both findings as corrected with focused re-review
  pending;
- no production or dependency file differs from `f23a0d5`.

Run these focused checks from the repository root:

    git diff --check
    rg -n '/Users/[^/]+/\.codex/' docs
    rg -n '\$\{CODEX_HOME:-\$HOME/\.codex\}/skills/\.system/skill-creator' docs/plans/archives
    rg -n '^#\[cfg\(test\)\]|^mod tests' crates/market/src/quote.rs
    git diff --quiet f23a0d5 -- crates Cargo.toml Cargo.lock
    bash -n scripts/stage-release-package-files.sh
    cmp -s AGENTS.md CLAUDE.md

The first `rg` must produce no matches. The portable-path search should find
exactly ten commands across the three archived plans. Full Rust tests, clippy,
and live tests do not need to be repeated because no Rust or dependency file
changed.

Report findings first with tight file and line references. If both corrections
are accurate and no new documentation contradiction appears, say explicitly
that focused re-review is green and the audit may be committed before v0.26
release readiness. Include the commands run and their outcomes. Do not modify
the worktree.
