# Review prompt: canonical-history final preflight

Perform a read-only review of the canonical-history sanitation final preflight.
Do not edit files, stage changes, commit, rewrite the working repository,
disable workflows, push refs, delete backups, or mutate GitHub state.

The tracked recurrence guard and recovery notice are already independently
reviewed and committed. The final preflight is generated under the ignored
`target/history-sanitation-preflight/` directory from the freeze commit named
in `summary.json`. Its rollback repository and bundle intentionally retain the
old canonical history and must remain private.

Read:

- `docs/plans/tradingview-cli-canonical-history-sanitation.md`
- `docs/v0.26-roadmap.md`
- `docs/v0.26-work-items.md`
- `CONTINUITY.md`
- `target/history-sanitation-preflight/run_preflight.py`
- `target/history-sanitation-preflight/summary.json`
- normalized ref, tag, workflow, and Release manifests under that directory
- bundle verification and atomic push dry-run logs under that directory

Do not print or quote `replacements.txt`, historical blobs, raw bundle content,
or removed private values. Aggregate counts and object IDs are acceptable.

Confirm:

- `summary.json` source commit equals current `main`, source and rewritten trees
  are equal, and exactly the expected historical cleanup commit is pruned;
- the rewrite repository contains only main and 28 tags before adding its one
  verified `canonical` remote;
- no legacy home, complete legacy validator path, or concrete username
  alternation remains reachable from rewritten main or tags;
- all lightweight tags follow the commit map, and annotated `v0.1.0` preserves
  type, name, tagger, and complete message while its target follows the map;
- the rollback repository fetches the exact 29 canonical remote refs, the
  mode-0600 bundle verifies, and normalized bundle heads exactly equal the
  captured remote direct-ref manifest;
- the normalized manifest contains 27 Releases and 135 assets with the required
  IDs, names, sizes, and digests;
- the release workflow was active with zero queued or in-progress runs;
- filter execution removes remotes, then one `canonical` remote is added and
  its fetch URL, push URL, repository identity, and remote manifest are checked;
- the dry-run uses `--atomic`, exactly 29 explicit leases, exactly 29 explicit
  refspecs, and the verified `canonical` remote, with no broad force or wildcard;
- no workflow or ref mutation occurred.

Also review the generated forward and rollback mutation scripts once present.
They must implement the pre-armed workflow restoration handler, pre/post-disable
run checks, finite state polling, 29 leased atomic updates, emergency enable
command, separate rollback approval, and public-safe output described by the
ExecPlan. If the scripts are not yet present, report that artifact review is
incomplete rather than authorizing external mutation.

Run focused read-only checks as useful, including:

    python scripts/check-public-hygiene.py
    git diff --check
    git status --short --branch
    git -C target/history-sanitation-preflight/rewrite.git fsck --full
    git -C target/history-sanitation-preflight/rollback.git \
      bundle verify ../canonical-before.bundle

Report findings first with file and line references. If no findings remain,
state whether the project owner may be shown the exact mutation summary for a
separate explicit approval. Never perform that mutation during review.
