# Review prompt: repository-wide machine-specific path cleanup

Perform a read-only review of the uncommitted documentation cleanup after
commit `51600f8`. Do not edit files, stage changes, commit, or push.

The issue is that historical validation regexes embedded a real local username
beside a generic `/Users/` detector. The username-specific branch was redundant
because `/Users/` already matches every macOS user path. The cleanup should
remove 79 username occurrences across 61 tracked documentation files without
changing the remaining validation pattern or any Rust/Cargo file.

Read:

- `docs/plans/tradingview-cli-machine-specific-path-cleanup.md`
- `docs/v0.26-roadmap.md`
- `docs/v0.26-work-items.md`
- `docs/plans/README.md`
- `CONTINUITY.md`
- `CHANGELOG.md`

Check that every mechanically changed historical validation line differs only
by removal of the username-specific alternation and still includes the generic
`/Users/` detector. Confirm that deliberate `/Users/example` Rust fixtures are
synthetic sanitization-test inputs and not machine-specific data.

Run from the repository root:

    git grep -nE '/Users/[[:alnum:]_.-]+\|/Users/' -- .
    git diff --check
    git diff --quiet 51600f8 -- crates Cargo.toml Cargo.lock
    bash -n scripts/stage-release-package-files.sh
    cmp -s AGENTS.md CLAUDE.md

The concrete-username alternation search must return no matches, and the
Rust/Cargo diff check must succeed. Inspect the full documentation diff for
accidental changes beyond the mechanical regex simplification and the new
cleanup planning/handoff records.

Report findings first with tight file and line references. Distinguish a real
machine-specific path from a generic detection pattern or explicit synthetic
placeholder. If no findings remain, state whether this cleanup may be committed
and v0.26 release readiness may begin. Include commands and outcomes. Do not
modify the worktree.
