# Focused re-review prompt: canonical Git-history sanitation plan

Perform a read-only review of the uncommitted canonical-history sanitation
plan. Do not edit files, stage changes, commit, push, disable workflows, or
change GitHub refs.

The current tracked tree was cleaned and committed as `97949d7`. The new plan
proposes rewriting canonical `main` and 28 release tags, temporarily disabling
the tag-triggered release workflow, preserving a private rollback bundle,
verifying 27 GitHub Release objects, adding a recurrence guard, and publishing
downstream clone-recovery guidance.

Read:

- `docs/plans/tradingview-cli-canonical-history-sanitation.md`
- `docs/v0.26-roadmap.md`
- `docs/v0.26-work-items.md`
- `docs/plans/README.md`
- `CONTINUITY.md`
- `.github/workflows/release.yml`
- `.github/workflows/ci.yml`

The first review found seven issues, and the first focused re-review found three
additional issues. Re-review only the three latest corrections plus any direct
conflict they create. A full source-code review is unnecessary. Confirm:

- the concrete synthetic home prefix appears only in the two allowed source
  fixtures and not in this ExecPlan or another tracked document;
- restoration handlers and recovery-required state are installed before the
  disable request, current workflow state is checked during cleanup, and
  queued/in-progress runs are checked again after disable and before push;
- after `git-filter-repo` removes `origin`, the rewrite repository adds one
  dedicated `canonical` remote, verifies its fetch/push URL and identity, and
  uses that same remote for dry-run and real push.

Also confirm that the original seven corrections remain intact:

- only canonical `main` and `refs/tags/*` are rewritten and pushed;
- local `refs/codex`, remote-tracking, backup, and filter refs are excluded;
- replacement values are dynamically derived and never written into tracked
  files;
- the rollback repository fetches the exact canonical remote main and 28 tags,
  and `bundle list-heads` matches the captured 29 direct refs and OIDs;
- the forward push uses exactly 29 explicit refspecs and 29 per-ref leases,
  with no broad force flag or wildcard, so a concurrent update fails atomically;
- workflow handling asserts active/no queued or running release run, arms a
  restoration trap before disable, polls disabled/active states, and provides
  an emergency enable path;
- rollback has separate approval and repeats the workflow guard plus a leased
  atomic restore, never updating tags while the release workflow is active;
- the expected pruning of empty commit `3ad570b` is explicit;
- all tags preserve direct OID, peeled commit, and tree evidence; the annotated
  tag additionally preserves its message, tagger metadata, and peeled target;
- the canonical sorted Release manifest contains the specified release fields
  and per-asset ID/name/size/digest fields and is compared byte-for-byte;
- existing clones receive actionable fresh-clone and optional explicit
  realignment guidance, including backup and old-SHA/worktree/submodule impact;
- recurrence detection and self-tests run on both Ubuntu and Windows and permit
  only the two exact path-and-value synthetic fixtures;
- explicit project-owner approval is required immediately before external
  workflow or ref mutation;
- the plan does not promise deletion from forks, caches, or third-party clones.

Also confirm the roadmap, work inventory, plan status, and continuity ledger
all say that focused plan re-review precedes guard implementation and final
preflight.

You may repeat local read-only inventory commands and create disposable data
under `target/`, but do not run a real rewrite against the working repository or
perform any external mutation. Report findings first with file and line
references. If no findings remain, state whether the preparatory guard and
migration-notice implementation may begin. Final preflight remains a later
step after those changes are independently reviewed and committed. Include
commands and outcomes.
