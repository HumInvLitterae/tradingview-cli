# Review prompt: canonical-history preparation implementation

Perform a read-only review of the uncommitted recurrence guard and downstream
recovery notice. Do not edit files, stage changes, commit, rewrite history,
disable workflows, push refs, or mutate GitHub state.

The canonical-history sanitation ExecPlan passed two plan-review rounds. This
implementation step adds only preparatory safeguards and documentation. Final
rewrite preflight and external mutation have not started.

Read:

- `scripts/check-public-hygiene.py`
- `.github/workflows/ci.yml`
- `docs/development.md`
- `docs/history-rewrite-recovery.md`
- `README.md`
- `docs/plans/tradingview-cli-canonical-history-sanitation.md`
- `docs/v0.26-roadmap.md`
- `docs/v0.26-work-items.md`
- `docs/plans/README.md`
- `CHANGELOG.md`
- `CONTINUITY.md`

Confirm the guard:

- uses only the Python standard library and scans `git ls-files -z` text;
- reads symlink target text without following a directory symlink, skips binary
  files, and handles deleted index entries during local review;
- detects concrete macOS and Linux homes, Windows backslash and slash profile
  paths, and the historical username-specific macOS detector alternation;
- constructs detector and self-test values without embedding a new concrete
  home path in the script or docs;
- permits only one exact synthetic fixture line in each of the two designated
  Rust source files, rejects the same value elsewhere, and rejects another
  value in an allowed file;
- reports only repository-relative path, line, and detector category rather
  than echoing a matched value;
- exits zero for a clean tree, one for policy violations, and two for repository
  read failures;
- runs self-test and tracked-tree scan in both Ubuntu and Windows script-check
  jobs without an OS condition.

Confirm the recovery notice:

- clearly says the rewrite has not happened yet and commands must wait for a
  maintainer completion announcement;
- recommends a fresh clone and forbids merging or pulling old and rewritten
  histories;
- inventories dirty state, unpushed commits, local refs, worktrees, untracked
  files, and submodules before recovery;
- creates a private bundle plus staged and unstaged patches outside the clone;
- explains safe content-level transfer and runs the hygiene guard before a new
  commit;
- keeps existing-clone realignment optional, requires a clean `main` and bundle,
  fetches main and tags into a temporary namespace, separates inspection from
  reset, and updates only manifest-listed canonical tags;
- explains old SHA, PR, worktree, submodule, fork, cache, and third-party clone
  limitations without claiming global deletion.

Run at least:

    python scripts/check-public-hygiene.py --self-test
    python scripts/check-public-hygiene.py
    python -m py_compile scripts/check-public-hygiene.py
    git diff --check
    bash -n scripts/stage-release-package-files.sh
    cmp -s AGENTS.md CLAUDE.md

Because new files are untracked, also inspect them directly rather than relying
only on `git diff`. Confirm that the concrete synthetic fixture value appears
only in the two designated Rust files. Rust and Cargo sources should be
unchanged from `97949d7`; full Rust tests are not required for this
Python/docs/CI-only slice.

Report findings first with file and line references. If no findings remain,
state whether the preparatory changes may be committed and whether the next
step may create the final dry-run/preflight artifacts. Include commands and
outcomes.
