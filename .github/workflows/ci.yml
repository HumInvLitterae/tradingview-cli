name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  script-check:
    name: Script checks
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Check public hygiene
        run: |
          python scripts/check-public-hygiene.py --self-test
          python scripts/check-public-hygiene.py
      - name: Check shell scripts
        if: runner.os != 'Windows'
        run: |
          bash -n scripts/git-hooks/pre-commit-fast.sh
          bash -n scripts/git-hooks/pre-push-baseline.sh
          bash -n scripts/install-config-hooks.sh
      - name: Check PowerShell scripts
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $tokens = $null
          $errors = $null
          [System.Management.Automation.Language.Parser]::ParseFile("scripts/git-hooks/pre-commit-fast.ps1", [ref]$tokens, [ref]$errors) | Out-Null
          if ($errors.Count -gt 0) { throw $errors[0] }
          [System.Management.Automation.Language.Parser]::ParseFile("scripts/git-hooks/pre-push-baseline.ps1", [ref]$tokens, [ref]$errors) | Out-Null
          if ($errors.Count -gt 0) { throw $errors[0] }
          [System.Management.Automation.Language.Parser]::ParseFile("scripts/install-config-hooks.ps1", [ref]$tokens, [ref]$errors) | Out-Null
          if ($errors.Count -gt 0) { throw $errors[0] }

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Check formatting
        run: cargo fmt --check

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - name: Run clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

  study-values-js-contract:
    name: Study-values JavaScript contract
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.18.0"
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Run study-values JavaScript contract
        run: python3 scripts/check-study-values-js-contract.py

  pine-open-js-contract:
    name: Pine open JavaScript contract
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.18.0"
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Run Pine open JavaScript contract
        run: python3 scripts/check-pine-open-js-contract.py

  indicator-insertion-js-contract:
    name: Indicator insertion JavaScript contract
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.18.0"
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Run indicator insertion JavaScript contract
        run: python3 scripts/check-indicator-insertion-js-contract.py

  three-point-drawing-js-contract:
    name: Three-point drawing JavaScript contract
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.18.0"
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Run three-point drawing JavaScript contract
        run: python3 scripts/check-three-point-drawing-js-contract.py

  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Run tests
        run: cargo test --workspace
