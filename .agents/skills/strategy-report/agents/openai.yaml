interface:
  display_name: "Strategy Report"
  short_description: "Review strategy context with CLI gaps."
  default_prompt: "Use strategy-report to review TradingView strategy context with current Rust tv CLI capabilities and gaps."
