[package]
name = "tradingview-scanner"
version = "0.2.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
reqwest = { version = "0.13.3", features = ["json"] }
serde_json = "1.0.149"
tradingview-core = { path = "../core" }
