interface:
  display_name: "Replay Practice"
  short_description: "Run bounded TradingView Replay practice."
  default_prompt: "Use $replay-practice for a bounded practice session with an explicit end state."
